import { Hashtag } from "./hashtag";

export class SelectedHashtag extends Hashtag {
    source: string; // mostRelevant | trending | user

    // public constructor(init?: Partial<SelectedHashtag>) {
    //   Object.assign(this, init);
    // }

}